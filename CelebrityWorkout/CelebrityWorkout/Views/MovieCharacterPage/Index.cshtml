@model List<CelebrityWorkout.Models.MovieCharacterDTO>

<h2>Movie Characters</h2>

<a asp-action="Create" class="btn btn-primary mb-3">Add New Character</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Movie</th>
            <th>Year</th>
            <th>Celebrity</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CharacterName</td>
                <td>@item.MovieTitle</td>
                <td>@item.YearOfRelease</td>
                <td>@item.Celebrity?.Name</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CharacterId" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.CharacterId" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.CharacterId" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
